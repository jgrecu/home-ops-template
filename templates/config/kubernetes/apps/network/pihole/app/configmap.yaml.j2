---
apiVersion: v1
kind: ConfigMap
metadata:
  name: pihole-dnsmasq-config
data:
  02-conditional-forward.conf: |
    # Conditional forwarding for cluster domain
    # Forward *.${SECRET_DOMAIN} queries to k8s_gateway
    server=/${SECRET_DOMAIN}/#{ cluster_dns_gateway_addr }#
