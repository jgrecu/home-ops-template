---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: woodpecker-server
spec:
  # Must wait for the CNPG operator so the Cluster CRD exists before postgres.yaml is applied
  dependsOn:
    - name: cloudnative-pg
      namespace: database-system
  interval: 1h
  path: ./kubernetes/apps/woodpecker/woodpecker-server/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: woodpecker
  wait: true
